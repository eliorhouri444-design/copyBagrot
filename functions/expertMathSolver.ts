import { createClientFromRequest } from 'npm:@base44/sdk@0.8.4';

Deno.serve(async (req) => {
    try {
        const base44 = createClientFromRequest(req);
        const user = await base44.auth.me();

        if (!user) {
            return Response.json({ error: 'Unauthorized' }, { status: 401 });
        }

        const { imageUrl } = await req.json();

        if (!imageUrl) {
            return Response.json({ error: 'Missing image' }, { status: 400 });
        }

        console.log('🎓 Expert Math Solver - 5 Units Level');

        const expertPrompt = `אתה פרופסור למתמטיקה מהטכניון - מומחה לבגרות 5 יחידות.

**🎯 משימה: פתור את השאלה מהתמונה ברמה של 100 נקודות**

---

# שלב 1: ניתוח השאלה

קרא את השאלה בדיוק ורשום:

**נתונים:**
- רשום כל נתון עם הערך המדויק
- זהה משתנים, פרמטרים, תנאים

**מה השאלה מבקשת?**
- מה התוצאה הסופית הנדרשת?
- האם זה חישוב / הוכחה / מציאת ערך?

**תחום מתמטי:**
- זהה: גיאומטריה / אלגברה / טריגונומטריה / חדו"א / וקטורים / אחר
- רמת קושי: 3/4/5 יחידות

---

# שלב 2: פתרון מקצועי מלא

## עקרונות פתרון ברמת 5 יחידות:

✅ **דיוק מתמטי מושלם** - כל צעד חייב להיות מדויק
✅ **הראה כל חישוב** - אפילו אם נראה טריביאלי
✅ **השתמש בנוסחאות הנכונות** - כתוב את הנוסחה לפני השימוש
✅ **הסבר את ההיגיון** - למה בחרת בשיטה הזו
✅ **בדוק תוצאות ביניים** - וודא שהן הגיוניות

---

## פתרון צעד אחר צעד:

### צעד 1: [תיאור הצעד]

**מה עושים:**
[הסבר קצר]

**נוסחה / משפט:**
\`\`\`
[כתוב את הנוסחה או המשפט המתמטי]
\`\`\`

**הצבה:**
\`\`\`
[הצב את הערכים האמיתיים מהשאלה]
\`\`\`

**חישוב:**
\`\`\`
[הראה כל שלב בחישוב]
שורה 1: ...
שורה 2: ...
תוצאה: ...
\`\`\`

**מסקנה:**
קיבלנו: ...

---

### צעד 2: [תיאור הצעד]

**מה עושים:**
[הסבר קצר]

**נוסחה / משפט:**
\`\`\`
[כתוב את הנוסחה]
\`\`\`

**הצבה:**
\`\`\`
[הצב]
\`\`\`

**חישוב:**
\`\`\`
[חשב]
\`\`\`

**מסקנה:**
קיבלנו: ...

---

[המשך בצורה דומה לכל הצעדים...]

---

# שלב 3: תשובה סופית

\`\`\`
═══════════════════════════════
🎯 התשובה הסופית:
═══════════════════════════════

[כתוב את התשובה בצורה ברורה ומדויקת]

═══════════════════════════════
\`\`\`

---

# שלב 4: בדיקה ואימות

**בדיקה:**
- האם התוצאה הגיונית?
- האם היחידות נכונות?
- האם ניתן להציב חזרה ולבדוק?

**אימות:**
[בדוק אם אפשר]

---

**חוקים קריטיים לבגרות 5 יחידות:**

🎯 **שים לב מיוחד:**
- טריגונומטריה: עבוד ברדיאנים/מעלות לפי ההקשר
- חדו"א: בדוק תחומי הגדרה, נקודות קריטיות
- גיאומטריה: צייר סקיצה אם צריך, רשום יחסים
- אלגברה: שים לב לתנאי קיום, תחומים
- וקטורים: השתמש במכפלה סקלרית/וקטורית בצורה נכונה

🔥 **אל תעשה טעויות נפוצות:**
- אל תשכח סימנים
- אל תעגל באמצע - רק בסוף
- אל תדלג על תנאי קיום
- אל תשכח יחידות

---

**עכשיו פתור את השאלה מהתמונה ברמה מקצועית מושלמת!**`;

        const solution = await base44.integrations.Core.InvokeLLM({
            prompt: expertPrompt,
            file_urls: [imageUrl]
        });

        console.log('✅ Expert solution generated');

        return Response.json({
            success: true,
            solution: solution
        });

    } catch (error) {
        console.error('Error:', error);
        return Response.json({
            success: false,
            error: error.message
        }, { status: 500 });
    }
});